{
  "name": "financeai-pro",
  "version": "0.2.0",
  "private": true,
  "description": "Root workspace scripts — run the FastAPI server and Next.js client together.",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "node scripts/py.mjs uvicorn app.main:app --reload --port 8000",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "concurrently -n server,client -c blue,green \"npm run start:server\" \"npm run start:client\"",
    "start:server": "node scripts/py.mjs uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "start:client": "npm --prefix client run start",
    "test": "npm run test:server && npm run test:client",
    "test:server": "node scripts/py.mjs pytest -q",
    "test:client": "npm --prefix client exec tsc -- --noEmit",
    "migrate": "node scripts/py.mjs alembic upgrade head",
    "migrate:new": "node scripts/py.mjs alembic revision --autogenerate -m",
    "migrate:check": "node scripts/py.mjs alembic check"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
